# Repository execution instructions

Read and apply `.claude/rules/execution-contract.md` and `CLAUDE.md` before
substantive work. Preserve the established objective and accumulated state.
The supplied contract is installed in this repository.

Claude Code lifecycle integration is registered in `.claude/settings.json`;
its scope and receipt-based activation checks are in `fable-enforce/HOOKS.md`.
Other hosts must not claim that Claude Code hooks execute in their own runtime.
